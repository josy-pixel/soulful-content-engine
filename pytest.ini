[pytest]
# Put the project root on sys.path so tests can `import app` / `import database`
# regardless of how pytest is invoked (plain `pytest` in CI, or `python -m pytest`).
pythonpath = .
testpaths = tests
